---

ferm_input_rules:
  - "{{ ferm_input_rule_netdata }}"
  - "{{ ferm_input_rule_bastion }}"
  - saddr:
      - "{{ lookup('dig', 'lb.priv.karolak.fr') }}"
    proto:
      - tcp
    dport:
      - 80
